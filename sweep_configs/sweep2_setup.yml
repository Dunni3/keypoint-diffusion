method: grid
metric:
  goal: minimize
  name: test_l2_loss
parameters:
  n_keypoints:
    values:
      - 10
      - 15
      - 20
  n_convs_encoder:
    values:
      - 2
      - 4
      - 6
  rec_encoder_loss_weight:
    values:
      - 0.0
      - 0.1
program: train_crossdocked.py
command:
  - /net/dali/home/mscbio/icd3/.conda/envs/ligdiff/bin/python
  - ${program}
  - "--config=configs/sweep2.yml"
  - ${args}