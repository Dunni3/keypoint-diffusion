method: grid
metric:
  goal: minimize
  name: test_l2_loss
parameters:
  rec_enc_loss_type:
    values:
      - 'optimal_transport'
      - 'gaussian_repulsion'
  rec_encoder_loss_weight:
    values:
      - 0.1
      - 0.01
  keypoint_feats:
    values:
      - 128
      - 256

program: train_crossdocked.py
command:
  - /net/dali/home/mscbio/icd3/.conda/envs/ligdiff/bin/python
  - ${program}
  - "--config=configs/sweep_repulsion.yml"
  - ${args}