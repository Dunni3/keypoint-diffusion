command:
  - /net/dali/home/mscbio/icd3/.conda/envs/ligdiff/bin/python
  - ${program}
  - --config=configs/ip_noatom.yml
  - ${args}
method: grid
metric:
  goal: minimize
  name: test_l2_loss
parameters:
  max_fake_atom_frac:
    values:
      - 0.1
      - 0.2
      - 0.4
  batch_size:
    values:
      - 64
      - 200
  lr:
    values:
      - 1.0e-4
      - 4.0e-4
program: train_crossdocked.py