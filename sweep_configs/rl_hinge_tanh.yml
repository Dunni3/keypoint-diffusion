method: grid
metric:
  goal: minimize
  name: test_l2_loss
parameters:
  rl_hinge_loss_weight:
    values:
      - 10
      - 1000
  rl_dist_threshold:
    values:
      - 3
  use_tanh:
    values:
      - false
      - true

program: train_crossdocked.py
command:
  - /net/dali/home/mscbio/icd3/.conda/envs/ligdiff/bin/python
  - ${program}
  - "--config=configs/rl_hinge.yml"
  - ${args}
