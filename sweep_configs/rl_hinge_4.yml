method: grid
metric:
  goal: minimize
  name: test_l2_loss
parameters:
  rl_hinge_loss_weight:
    values:
      - 0.01
      - 0.001
  rl_dist_threshold:
    values:
      - 1
      - 2
  n_convs_encoder:
    values:
      - 6
      - 8

program: train_crossdocked.py
command:
  - /net/dali/home/mscbio/icd3/.conda/envs/ligdiff/bin/python
  - ${program}
  - "--config=configs/rl_hinge_4.yml"
  - ${args}
